apply plugin: 'com.android.library'

android {
    compileSdkVersion 27
    buildToolsVersion '27.0.3'

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 22
        versionCode 31
        versionName '1.7.1'
        ndk {
            abiFilters 'armeabi-v7a', 'x86'
        }
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation 'com.facebook.react:react-native:+'
    implementation 'com.microsoft.appcenter:appcenter-crashes:1.7.0'

    // compile project(':AppCenterReactNativeShared')   // For testing with TestApp
    implementation 'com.microsoft.appcenter.reactnative:appcenter-react-native:1.7.1'
}
