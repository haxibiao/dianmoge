import {
	ServerRoot,
	name as AppName,
	version as AppVersion,
	displayName as AppDisplayName,
	Spinner,
	AppSlogan
} from "../app.json";

export default {
	ServerRoot,
	AppName,
	AppDisplayName,
	AppVersion,
	Spinner,
	AppSlogan
};
